server:
  port: 8080

spring:
  application:
    name: employee-security-backend

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/employee-security

logging:
  level:
    org.springframework.security: INFO